from pyspark.sql import functions as F

df = df.withColumn(
    "concatenated_column",
    F.concat_ws(
        "/", 
        F.coalesce(F.col("column1"), F.lit("")),  # Replace NULL with empty string
        F.coalesce(F.col("column2"), F.lit("")),  # Replace NULL with empty string
        F.coalesce(F.col("column3"), F.lit(""))   # Replace NULL with empty string
    )
)