UPDATE frtb_latest.fpav_attribute_map
SET EBA_PNL_CATEGORY = 'PC1'
WHERE ATTRIBUTE_ID IN (
    SELECT ATTRIBUTE_ID 
    FROM (
        SELECT ATTRIBUTE_ID
        FROM frtb_latest.fpav_attribute_map
        ORDER BY RANDOM()
        LIMIT 179
    ) subquery
);